{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}


{% block body %}
    <div class="flex justify-center mt-[20vh]">
        <form method="post" class="xl:w-1/3 min-w-[100px] rounded-xl border-gray-50 border-2 p-10 drop-shadow-lg">

            <h1 class="text-3xl text-center -mt-5 mb-5">Please sign in</h1>
            {% if error %}
                <div class="rounded-md w-full p-2 mb-5 text-sm text-red-700 bg-red-50 drop-shadow-none">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            {% if app.user %}
                <div class="rounded-md w-full p-2 mb-5 text-sm text-green-700 bg-green-50 drop-shadow-none">
                    You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}" class="text-blue-700">Logout</a>
                </div>
            {% endif %}

            <label for="inputEmail" class="block after:content-['*'] after:text-red-700 font-semibold mb-0.5">Email </label>
            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="w-full text-sm text-gray-700 bg-gray-50 focus:bg-white transition duration-1000 focus:outline-cyan-600 rounded-md mb-2 p-1 border-gray-50 border-2 drop-shadow-sm" autocomplete="email" required autofocus>

            <label for="inputPassword" class="block after:content-['*'] after:text-red-700 font-semibold mb-0.5">Password </label>
            <input type="password" name="password" id="inputPassword" class="w-full text-sm text-gray-700 bg-gray-50 focus:bg-white transition duration-1000 focus:outline-cyan-600 rounded-md mb-2 p-1 border-gray-50 border-2 drop-shadow-sm" autocomplete="current-password" required>
            <br>
            <input type="hidden" name="_csrf_token"
                   value="{{ csrf_token('authenticate') }}"
            >

            {#
            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html

            <div class="checkbox mb-3">
                <label>
                    <input type="checkbox" name="_remember_me"> Remember me
                </label>
            </div>
            #}

            <button class="rounded-xl bg-cyan-700 hover:bg-cyan-600 w-full p-1 mt-2 text-center text-white font-semibold transition duration-1000" type="submit">
                Sign in
            </button>
        </form>
    </div>

{% endblock %}
